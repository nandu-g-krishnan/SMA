# Portfolio Construction Catalog

## Asset Allocation

**Purpose:** Capture Asset Allocation as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Sector Caps

**Purpose:** Capture Sector Caps as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Position Sizing

**Purpose:** Capture Position Sizing as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Correlation Control

**Purpose:** Capture Correlation Control as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Rebalancing

**Purpose:** Capture Rebalancing as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Tax Awareness

**Purpose:** Capture Tax Awareness as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Risk Parity

**Purpose:** Capture Risk Parity as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Drawdown Control

**Purpose:** Capture Drawdown Control as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.
