# Options Catalog

## Moneyness

**Purpose:** Capture Moneyness as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Intrinsic Value

**Purpose:** Capture Intrinsic Value as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Time Value

**Purpose:** Capture Time Value as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Delta

**Purpose:** Capture Delta as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Gamma

**Purpose:** Capture Gamma as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Theta

**Purpose:** Capture Theta as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Vega

**Purpose:** Capture Vega as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Implied Volatility

**Purpose:** Capture Implied Volatility as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Open Interest

**Purpose:** Capture Open Interest as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Put Call Ratio

**Purpose:** Capture Put Call Ratio as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Vertical Spreads

**Purpose:** Capture Vertical Spreads as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Iron Condor

**Purpose:** Capture Iron Condor as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Straddle

**Purpose:** Capture Straddle as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Strangle

**Purpose:** Capture Strangle as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.
