# Futures Catalog

## Contract Specs

**Purpose:** Capture Contract Specs as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Lot Size

**Purpose:** Capture Lot Size as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Expiry

**Purpose:** Capture Expiry as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Basis

**Purpose:** Capture Basis as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Rollover

**Purpose:** Capture Rollover as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Mark to Market

**Purpose:** Capture Mark to Market as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Margin

**Purpose:** Capture Margin as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Open Interest

**Purpose:** Capture Open Interest as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Cost of Carry

**Purpose:** Capture Cost of Carry as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Calendar Spread

**Purpose:** Capture Calendar Spread as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.
