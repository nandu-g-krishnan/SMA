# Risk Management Catalog

## Position Sizing

**Purpose:** Capture Position Sizing as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Stop Loss

**Purpose:** Capture Stop Loss as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## ATR Stop

**Purpose:** Capture ATR Stop as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Portfolio Heat

**Purpose:** Capture Portfolio Heat as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Max Drawdown

**Purpose:** Capture Max Drawdown as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Value at Risk

**Purpose:** Capture Value at Risk as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Expected Shortfall

**Purpose:** Capture Expected Shortfall as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Risk Reward

**Purpose:** Capture Risk Reward as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Correlation Risk

**Purpose:** Capture Correlation Risk as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.

## Gap Risk

**Purpose:** Capture Gap Risk as a first-class platform concept.

**Inputs:** Market data, reference data, document-derived rules, user configuration, and audit metadata.

**Outputs:** Normalized values, explanations, confidence scores, alerts, and strategy-ready features.

**Implementation Notes:** Model as versioned configuration plus tested computation/service modules. Store source traceability to the document knowledge index.

**Validation:** Unit tests, historical replay tests, edge-case fixtures, and monitoring for stale or anomalous values.
