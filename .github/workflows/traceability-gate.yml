name: Traceability Gate

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  traceability:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate traceability artifacts
        shell: bash
        run: bash automation/validate-traceability.sh
